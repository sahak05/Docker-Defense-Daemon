import time 
from flask import Flask, request, jsonify
import docker
import subprocess
import json

app = Flask(__name__)


if __name__ == "__main__"
import threading
app.run(host="0.0.0.0", port=8080)